\documentclass[english,b5paper,twoside,onecolumn,9pt]{memoir}
	\usepackage[english]{babel}
	\usepackage[T1]{fontenc}	
	\usepackage[utf8]{inputenc}

%% Figures
	\usepackage{graphicx}
	\usepackage[labelfont=bf,textfont={it},size=small]{caption}
	\usepackage[labelfont=bf,textfont={it}]{subcaption}
	\usepackage{circuitikz}
	\usepackage{pgfplots}
		\newlength\figureheight
		\newlength\figurewidth
		\pgfplotsset{compat=newest}
		\usepgfplotslibrary{external}
		\usepgfplotslibrary{groupplots}
		\usetikzlibrary{pgfplots.external}
		\usetikzlibrary{fadings}
		\tikzexternalize % Used to externalize tikz figure such they are not recreated every day
		%\tikzset{external/force remake} % Use when all figure must be remade
		\tikzsetexternalprefix{tikz2pdfBook/}

%% Index
\usepackage{idxlayout}
\makeindex
		
%% Math
	\usepackage{mathtools,mathrsfs} 
	\usepackage{amsmath,amssymb}
	\usepackage{bbold}
	\usepackage{bm,bbm}
	\usepackage{siunitx}

%% Fonts
	\usepackage{palatino}
	\usepackage{mathpazo}
	\DeclareSymbolFont{CMletters}{OML}{cmm}{m}{it}
	\DeclareMathSymbol{\xi}{\mathord}{CMletters}{"18}
	\usepackage{csquotes}
	\usepackage{multirow}

%% Drafts
	\usepackage{lipsum}
	\usepackage[draft]{fixme}
	\usepackage{xspace}

%% Pagesetup
	\usepackage[bottom=3.6cm,top=3cm]{geometry}
	\linespread{1.15}
	\usepackage{microtype}
	\let\footruleskip\undefined
	\usepackage{fancyhdr}
		\fancyhf{}
		\fancyhead[LE]{\itshape \nouppercase{\leftmark}}
		\fancyhead[RO]{\itshape \nouppercase{\rightmark}}
		\fancyfoot[LE]{\thepage}
		\fancyfoot[RO]{\thepage}
		\renewcommand{\headrulewidth}{0pt}
	

%% Bibliography
	\usepackage[backend=biber,sorting=none,maxbibnames=4,style=numeric]{biblatex}
	\addbibresource{references.bib}

%% Links and refs
	\usepackage{url}
	\usepackage[colorlinks=true,linkcolor=black,citecolor=black,urlcolor=black]{hyperref}
	\usepackage[capitalise]{cleveref}

%% Layout
	\setsecnumdepth{subsection}
	\setcounter{tocdepth}{2}
	\chapterstyle{veelo}
	%\chapterstyle{ell}

%% For quote
	\epigraphfontsize{\small\itshape}
	\setlength\epigraphwidth{8cm}
	\setlength\epigraphrule{0pt}